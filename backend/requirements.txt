# HireTrack backend dependencies
# Core framework
django>=5.0,<5.2
djangorestframework>=3.15

# Database
psycopg2-binary>=2.9

# Config / environment
python-dotenv>=1.0

# API plumbing
django-cors-headers>=4.3
django-filter>=24.1

# Resume parsing (used in a later task)
pdfplumber>=0.11
python-docx>=1.1
anthropic>=0.39

# Reports / exports
openpyxl>=3.1
reportlab>=4.1

# Production / deployment
gunicorn>=21.2            # WSGI server (Render start command)
whitenoise>=6.6           # serve collected static files from the web process
django-storages>=1.14     # S3-compatible media storage (Cloudflare R2)
boto3>=1.34               # S3 client used by django-storages
