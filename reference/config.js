/* ============================================================
   HIRE TRACK — configuration
   Paste your Supabase project's values below.
   Find them in Supabase → Project Settings → API.
   These two values are safe to expose in a public site.
   ============================================================ */
window.HIRETRACK_CONFIG = {
  url:     "https://YOUR-PROJECT-ref.supabase.co",   // "Project URL"
  anonKey: "YOUR-PUBLIC-ANON-KEY"                     // "anon / public" key
};
